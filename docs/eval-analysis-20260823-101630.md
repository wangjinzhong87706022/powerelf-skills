# 评估分析报告：run `hermes-eval-20260821-183134`

> 生成时间：2026-08-23 10:16
> 评估运行：2026-08-21 18:31 启动 → 2026-08-22 02:20 结束（约 7h49m）
> 数据来源：`output/hermes-eval-20260821-183134/`（JSON + MD + runner.log）+ `~/.hermes/state.db` 逐题 session 转录
> 对照基线：`output/hermes-eval-20260820-195312/`（0820 run，同口径）

---

## 1. 概览

| 字段 | 值 |
|---|---|
| 评测运行 ID | `hermes-eval-20260821-183134` |
| 实际评测题数 | 156（green 过滤后），总题池 290 |
| 总体得分 | **0.773（PASS）** |
| 结论分布 | PASS=114 / PARTIAL=36 / FAIL=6 |
| 模型 | Qwen3.8-27B-Q4_K_M（本地 GGUF） |
| 代码冻结 | 正常，0 题 CODE-MUTATED/DRY-RUN，冻结护栏生效 |
| 冻结基线快照 | `/home/scada/powerelf-eval-freeze/hermes-eval-20260821-183134-20260821-183134`（仓库外） |

跳过统计：placeholder_reconstructed 29、readiness_filter 99、empty_prompt_or_expected 6。

---

## 2. 与上次（0820 run）同口径对比

| 集合 | 0820 | 0821 | Δ | 0820 FAIL | 0821 FAIL |
|---|---|---|---|---|---|
| data-governance-routing-list | 0.723 | 0.734 | +0.011 | 0 | 4 |
| inspection-eval-cases | 0.770 | 0.805 | +0.035 | 2 | 0 |
| **inspection-eval-criteria** | 0.652 | **0.797** | **+0.145** | 3 | 1 |
| routing-evals-v1 | 0.648 | 0.708 | +0.060 | 0 | 0 |
| routing-evals-v2 | 0.740 | 0.783 | +0.043 | 0 | 1 |
| **OVERALL** | **0.735** | **0.773** | **+0.038** | | |

全线提升。`inspection-eval-criteria` 涨幅最大（+0.145）：判分器 A6 修复让函数名/关键词题恢复实质判分，0820 那 3 个 FAIL 中的 EVAL2/EVAL5 已转 PASS。

### 维度分布（0821）

| 维度 | 平均得分 | 失败题数（<0.4） |
|---|---|---|
| D1_functional | 0.731 | 17 |
| D2_routing | 0.614 | 14 |
| D3_tool_eff | 0.780 | 22 |
| D4_token_eff | 0.842 | 16 |
| D5_latency | 0.782 | 22 |
| D6_halluc | 0.987 | 2 |
| D7_completeness | 0.831 | 9 |

D6（幻觉）0.987 维度最健康；D2（路由）0.614 垫底，主要受判分器口径问题拖累（见下文类 A）。

---

## 3. 六个 FAIL 题：根因归类

逐题在 `state.db` 核对了 session 转录（prompt / final_answer / tool_call 链）+ master 题库 expected + 判分 note。6 个 FAIL 落在**三类根因**：

### 类 A：判分器关键词口径与 expected 语义不匹配（判分器侧 bug，非 skill 退化）

涉及 **DG-P40 / P41 / P42 / P43 / P46** —— 5 题 expected 是反引号函数名，判分器用 `pass_keywords` 做 AND 命中。

| 题号 | expected | 判分 note | D1 | D2 | D7 |
|---|---|---|---|---|---|
| DG-P40 | `generate_anomaly_report()` | pass_keywords | 0.0 | 0.0 | 1.0 |
| DG-P41 | `to_pdf()` | pass_keywords | 0.0 | 0.0 | 1.0 |
| DG-P42 | `generate_score_report()` | pass_keywords | 0.0 | 0.0 | 1.0 |
| DG-P43 | `fix_anomaly()` / `fill_missing()` | pass_keywords | 0.0 | 0.0 | 1.0 |
| DG-P46 | `batch_fix_anomalies()` | pass_keywords | 0.5 | 0.0 | 1.0 |

**实际转录显示 skill 回答全部正确**：

- **P41** 真的生成了 PDF（107KB / 2 页 / NotoSansCJK 中文渲染正常，内容含报告头/结论/设备明细表/甄别证据）。
- **P42** 真的跑了四维加权评分报告（质量35%+稳定10%+故障40%+完整15%，182 台设备，19 台可评分）。
- **P43** 真的写回 1464 条插值行 + 8 条 `eq_data_missing_record`，标记 `creator='data-governance-interpolation'` 并校验通过。
- **P46** 真的批量修复 165 个 MAD 异常点（z=69.53），写回 `eq_data_anomaly_record.id=191`。

但 D1/D2 都判 0。

**根因**：判分器把函数名当字面 token 在 `final_answer` 文本里找，但 agent 用的是**语义回答**（"PDF 已生成"/"写回完成"）而非复述函数名。判分 note `pass_keywords=[...]` + `d1=0.0` 说明判分器对"功能达成"的定义过窄——它判的是"回答里提了函数名"，不是"真的执行了该功能"。**D3/D7 给高分（功能确实完成）、D1/D2 给 0，自相矛盾**——这是判分器自相矛盾的指纹，不是 skill 退化。

### 类 B：超时导致的天花板（基础设施侧）

涉及 **routing-evals-v2 #23**（898.4s，触发 900s 超时）及 **DG-P38 / DG-P40 / DG-P43 / DG-P46**（均 450–760s）。

- **Q23** 水库水位插值题：agent 跑了 51 个 tool call / 98 条消息，正确识别 2 处大断档（1522h + 385h），但在"看 interpolate.py 写回机制"时撞上超时，未完成最终写回。判分器给 0.38。

**全量延迟统计**：median 115s、p90 447s、max 898s。**26 题超 300s、7 题超 600s**。延迟是这次分数的主要漏损源之一（D5_latency 有 22 题吃 0 地板）。

### 类 C：真实 skill 缺陷（需修代码）

只有 **EVAL9** 是真·skill bug，且 agent 自己在回答里精确诊断了出来。

- **EVAL9** prompt："水库水情、位移监测、MAD统计、关联异常这4个维度是否正确报告 OK？"
- expected：水情/MAD统计/关联异常 3 个维度报 OK 无 WARNING，位移维度报 CRITICAL（GNSS 测站速率 1.200mm/d > 1.0mm/d 为真实异常）。
- agent 核对报告 + 数据库 + 代码后判定："关联异常这个 OK 是假的"。

**根因**（agent 自诊，`impl/inspection_analyzer.py:1417-1422`）：
> `analyze_correlation` 把"辅助表（雨量 st_pptn_r）无数据"和"主表（水位 st_rsvr_r）无数据"混为一谈——对 7 天窗口有 1288 行的主表也整体返回 `no_data`，且 `no_data_result` 的探针查的是 st_pptn_r 的 MAX(tm) 而非 st_rsvr_r，**语义错乱**。

**连锁后果**：一致性闸把"关联异常"标 NO_DATA → 报告声称"15 项维度" vs 实际 6 项无数据 → 一致性闸未通过 → 整份报告置信度被降为 Needs revision。

**agent 的修复建议**（准确）：`analyze_correlation` 应区分三态——
- st_rsvr_r 空 → 真 NO_DATA（现状正确）
- st_rsvr_r 有 + 辅助表空 → **降级模式**：只跑可用的关联检查，finding 加 INFO"部分降级：雨量/渗压窗口无数据，相关检查跳过"，**不**整体 no_data

这是 [[inspection-report-fragmentation-empirical]] 里"置信度闸失效"问题的又一个具体落点。

### 低分 PARTIAL 补充

- **DG-P16 / P19**（三态判定 / 批量检测，0.45）：prompt 是"XX 设备当前是否在线"，agent 正确触发 clarify（问具体设备编码），判分器 `methods=[], task_type=judgment` 给 D1/D2=0。这是 clarify 题的判分口径问题——agent 识别歧义而非瞎答，`clarify_floor` 只在部分分支生效，这两题没触发。
- **DG-P32**（物理矛盾，0.385）：agent 回答质量很高（Pearson 相关 + 逐日均值，结论"渗压没真升、渗流没真降"），但 expected 是"物理矛盾检测"方法名，判分器判"没提方法词"→ D1/D2=0。又是类 A。

---

## 4. 延迟漏损

7 题超 600s 集中在 data-governance 的**重计算题**（插值写回、批量修复、报告生成），agent 反复 query 真实库 + 写临时脚本 + 读大文件。本地 Qwen3.8-27B Q4 在长 tool chain 下推理慢，叠加真实库查询。

| 指标 | 值 |
|---|---|
| median duration | 115.3s |
| p90 duration | 447.4s |
| max duration | 898.4s（Q23，撞 900s 超时） |
| >300s 题数 | 26 |
| >600s 题数 | 7 |
| >850s 题数 | 1 |

这不是 skill 逻辑问题，是算力/口径问题——可考虑对超长题设 `max_questions` 或给 agent 设工具预算上限，避免 900s 撞墙。

---

## 5. 结论与建议

| 优先级 | 动作 | 预期回收 |
|---|---|---|
| **P0** | 修 EVAL9 根因：`analyze_correlation` 三态判定 + 探针表名修正 | inspection-eval-criteria FAIL→PASS，一致性闸连锁恢复 |
| **P1** | 修判分器类 A：`pass_keywords` 题（反引号函数名）改为判"功能是否执行/输出语义命中"，而非字面函数名 AND。可复用 D7 完成度信号或加执行证据（tool_call 链中是否调用了对应函数） | DG-P40/41/42/43/46 共 5 题 D1/D2 解锁，data-governance 集合 +0.05~0.08 |
| **P1** | 修 clarify_floor 覆盖：DG-P16/P19 这类零 tool + 反问的 judgment/detection 题应吃 clarify 地板而非 0 | DG-P16/P19 从 0.45→0.6+ |
| **P2** | 延迟治理：对 >600s 的重计算题设工具调用上限或分段，避免 Q23 式超时 | routing-evals-v2 + DG 重计算题 D5 解锁 |

### 一句话总结

0821 评测真实进步 +0.038，6 个 FAIL 里 **5 个是判分器口径问题（skill 实际做对了）+ 1 个超时**，**只有 EVAL9 是真 skill bug**，且 agent 已自诊出根因和修复路径。判分器对"函数名题"和"clarify 题"的口径是当前最大的虚低来源。

---

## 附录：取证 session ID

| 题号 | session_id | 转录取证要点 |
|---|---|---|
| DG-P16 | `20260821_190545_8e763d` | 2 msg / 0 tool，agent clarify 问设备编码 |
| DG-P32 | `20260821_200332_016c8f` | 19 tool / 39 msg，Pearson + 逐日均值 |
| DG-P40 | `20260821_203134_164718` | 31 tool / 64 msg，月度异常报告生成 |
| DG-P41 | `20260821_203908_1ab110` | 31 tool / 60 msg，PDF 生成验证 |
| DG-P42 | `20260821_204339_b1f538` | 30 tool / 54 msg，四维评分报告 |
| DG-P43 | `20260821_205143_33ed9b` | 51 tool / 98 msg，插值写回 1464 行 |
| DG-P46 | `20260821_210727_ef562f` | 34 tool / 65 msg，批量修复 165 异常点 |
| routing-v2 #23 | `20260821_230113_58f79c` | 51 tool / 98 msg，898s 超时 |
| EVAL9 | `20260822_010515_d80f9c` | 62 tool / 122 msg，自诊 analyze_correlation 语义错乱 |

*本报告由人工 + state.db 转录核对生成，非 runner 自动产物。*

---

# 评审附注（2026-08-23 下午）

> 评审方式：对照三份独立证据逐项复核——两份 run 的 JSON 重算、judger/skill 源码精读、state.db 转录 + 实库（powerelf_srm_yml）复跑。

## 总评

**核心论点成立，数字层面高度可靠，但 3 处实质错误需修订，原 P0 建议按原文执行无法达成目标。**

中心判断——"6 FAIL 中 5 个判分器口径虚低 + 1 个超时，只有 EVAL9 是真 skill 问题；0.773 是真实进步"——经复核成立。所有头条数字、session 取证 ID、对比数据全部核实无误。问题集中在 EVAL9 根因表述及派生的 P0 建议。

## 核实无误 ✅

总体 0.773/156 题/PASS 分布、五集合分数与 Δ、七维度均值、延迟统计（median 115.3/p90 447.4/max 898.4/>300s=26/>600s=7）、9 个取证 session_id 及 tool/msg 计数、模型名（state.db 156 会话确认）、EVAL2/EVAL5 转 PASS（0.328→0.851 / 0.175→0.925）、类 A 五题"skill 实际做对"的转录证据、类 A 指纹 D1=D2=0+D7=1.0 自相矛盾——全部属实。

## 实质错误 ❌

### 错误 1：EVAL9"探针查错表"是 agent 误诊，本文照抄未验证

原文称"`no_data_result` 的探针查的是 st_pptn_r 而非 st_rsvr_r"。核查：

- `inspection_analyzer.py:1422` 明确传入 `"st_rsvr_r"`，`no_data_result` 探测的就是传入表（`:126`）
- 实库 `probe_table_latest(st_rsvr_r)`=**2026-08-21 18:00**（新鲜）；agent 引用的"最新 2026-08-14 03:00"实为 **st_pptn_r/st_pressure_r** 的 MAX，那些 Data Notes 本属雨量/渗压维度——agent 张冠李戴，本文未验证照抄

实证确认的真缺陷是：`analyze_correlation` 在辅助表窗口空时**静默跳过部分关联检查仍报 `[OK]`**（复跑实证），无降级披露。真 bug，机理不同。

### 错误 2：P0 不可达——修 analyze_correlation 无法让 EVAL9 转 PASS

EVAL9 expected 要求位移报 CRITICAL（GNSS 测站 97 速率 1.200mm/d > 1.0）。实库：

```
stn97 speed_gh 全史最大值 = 0.1228 mm/d（24 行）——期望的异常速率在数据中不存在
```

无论怎么修 `analyze_correlation`，位移都不可能报 CRITICAL。这是 [[mock-data-simulator-architecture]]"模拟器设计=EVAL9 答案须联动"契约失配。**P0 应拆两步**：①裁决题库-数据失配（重灌异常数据 or 改 expected 口径）；②修静默降级加 INFO 披露。

### 错误 3：P19 归类错误——没有 clarify，是"答对了被判 0"

P16 确实反问了设备编码 ✓（但用自然语言反问，2 msg/0 tool）。**P19 完全没 clarify**——跑 terminal 输出 633 台设备完整批量分级报告（CRITICAL 106 台…），正是 expected"批量检测"要求的行为，却因 expected 解析出 `methods=[]` + task_type 别名未命中被判 D1=0。P19 与 P32 同属 methods 空解析子 bug，不属 clarify 桶。

### 错误 4：clarify_floor 机理解释不准

floor 在分支链之后统一施加（runner:1374），不分分支。P16 没吃到的真实原因：`clarified` 只认 `tool_name=='clarify'` 消息（runner:1187-1189），P16 散文反问没调工具 → `clarified=False`。对照组 P46 note 带 `;clarify_floor` 恰因调了工具。

### 错误 5：§4 延迟集中性失实

7 题超 600s 构成：routing-v2 #23/#40/#17（3 题）、inspection ALERT-POS-1/CORR-NEG-1（2 题）、data-governance 仅 DG-P38/DG-P43（2 题）。非"集中在 data-governance"。且 routing #40 跑 809s 仍得 0.64——慢题≠失败题。

## 结构性瑕疵 ⚠️

1. FAIL/PARTIAL 账目错位：类 A 小节列的 P41(0.459)/P46(0.407) 是 PARTIAL，真 FAIL 的 P32 被放在"低分 PARTIAL 补充"下误标。
2. P32 指纹（D7=0、methods=[]）与 P40-43（D7=1.0、pass_keywords）不同源：前者属 methods/task_type 分支缺陷（与 P16/P19 同源），修法不同。
3. 小项：类 A 表 P46 行 D1=0.5 来自 clarify_floor 而非关键词半命中；"AND 命中"措辞不准（A6 后多函数名 `/` 分隔已是 OR，runner `_kw_any`）。

## 修订后的优先级

| 优先级 | 动作 | 相对原文 |
|---|---|---|
| **P0** | EVAL9 两步走：①裁决题库-数据失配（stn97 速率数据 vs expected 口径）②修 analyze_correlation 静默降级加 INFO 披露 | 原 P0 拆分重排 |
| **P1** | 判分器函数名题改判执行语义/tool-chain 证据（P40-43） | 不变 |
| **P1** | **新增**：methods=`[]` 空解析硬零修复（P16/P19/P32）——expected 解析失败应降级 neutral floor 而非 0 | 新增 |
| **P2** | clarify 语义扩展：prose 反问也计入 clarified，或 SKILL 引导歧义时调 clarify 工具 | 改写降级 |
| **P2** | 延迟治理范围修正：含 routing-v2 长链题（#17/#23/#40） | 对象修正 |

## 执行记录（2026-08-23 下午）

P0 两步已完成并验证：

1. **数据侧重灌**：模拟数据漂移实锤（验收闸 20 项红、5 问题设备全缺）→ `simulate_7d_data.py --seed 42` 全量重灌 + dsm 表孤儿行清理 → 验收闸 **exit 0 全绿**；EVAL9 四维冒烟通过（位移 CRITICAL 与 expected 逐字吻合；MAD min_samples=20 护栏使四维期望可同时满足）；一致性闸 Needs revision → Ready to share。
2. **代码侧修复**：`analyze_correlation` 辅助表空时由静默跳过改为逐项 INFO"部分降级"披露 + OK 注明部分覆盖；`test_inspection.py` 新增两用例锁行为（红→绿），全套 55 passed / 20 skipped。

⚠️ 口径提示：数据重灌后，inspection/data-governance 集合与历史基线（0.759/0.774/0.773）不再同口径，下次评测需重立锚点；评测启动规程新增"启动前必跑 verify_simulated_data.py exit 0"防线。
