# （已迁移）数据库 SQL 参考 → _shared/references/schema.md

> 本文件（原 12 类监测表 DDL、基础信息表、预警/数据源注册表、常见 SQL 模式）已合并至
> 跨 skill **单一事实源**：
>
> **[`../../_shared/references/schema.md`](../../_shared/references/schema.md)**
>
> 请直接阅读该文件。本文件保留仅为兼容既有引用链接，不再单独维护。
> 巡检业务实体（`business_check_*` 11 张表）仍在同目录 `data-model.md` 维护。
